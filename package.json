{
  "name": "idea-to-svg_the-gallery-updates-on-ai-gen-now",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:all:android": "npm run build:android && npx cap sync android && npm run cap:sync && cd android && set JAVA_HOME=C:\\Program Files\\Android\\Android Studio\\jbr&& gradlew assembleRelease assembleDebug bundleRelease",
    "build:web": "vite build",
    "build:android": "cross-env BUILD_TARGET=capacitor vite build",
    "build:ghpages": "tsc && vite build --mode ghpages",
    "deploy": "npm run build:ghpages && gh-pages -d dist -b gh-pages --add",
    "deploy:clean-cache": "gh-pages-clean",
    "deploy:reset-branch": "git push origin --delete gh-pages",
    "preview": "vite preview",
    "cap:sync": "npm run build:android && npx cap sync android",
    "cap:open": "npx cap open android",
    "build:aab": "npm run build:android && npx cap sync android && cd android && ./gradlew bundleRelease"
  },
  "dependencies": {
    "@aparajita/capacitor-secure-storage": "^8.0.0",
    "@capacitor/android": "^8.1.0",
    "@capacitor/core": "^8.1.0",
    "@capacitor/preferences": "^8.0.1",
    "@google/genai": "^1.41.0",
    "dompurify": "^3.3.1",
    "firebase": "^12.9.0",
    "lucide-react": "^0.564.0",
    "react": "^19.2.4",
    "react-dom": "^19.2.4",
    "uuid": "^13.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.1.0",
    "@types/node": "^22.14.0",
    "@vitejs/plugin-react": "^5.0.0",
    "cross-env": "^10.1.0",
    "gh-pages": "^6.3.0",
    "typescript": "~5.8.2",
    "vite": "^6.2.0"
  }
}
